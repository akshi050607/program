#include <stdio.h>

struct student {
    int roll;
    char name[20];
};

int main() {
    struct student s[50];
    int n;

    printf("Enter number of records: ");
    scanf("%d", &n);

    printf("Total Records Stored = %d\n", n);

    return 0;
}
