#include <stdio.h>

int main() {
    int i, n;

    printf("Enter number of students present: ");
    scanf("%d", &n);

    printf("Attendance Numbers:\n");

    for (i = 1; i <= n; i++) {
        printf("Student %d\n", i);
    }

    return 0;
}
