#include <stdio.h>

float area(float l, float b);

int main() {
    float l, b;

    printf("Enter length and breadth: ");
    scanf("%f %f", &l, &b);

    printf("Area = %.2f\n", area(l, b));
    return 0;
}

float area(float l, float b) {
    return l * b;
}
